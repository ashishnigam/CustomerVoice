{
  "name": "@customervoice/config",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "eslint 'src/**/*.ts'",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "echo 'config tests not scaffolded yet'",
    "test:integration": "echo 'config integration tests not scaffolded yet'",
    "test:e2e": "echo 'config e2e tests not scaffolded yet'"
  },
  "devDependencies": {
    "@types/node": "^22.10.2"
  }
}
